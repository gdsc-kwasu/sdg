import React from "react";

// this could be a component that we'll use to render the list of all the users on our platform
// it is also subject to change. As i haven't een the UI.
export default function UsersPage() {
  return (
    <div>
      <h1>Users page</h1>
    </div>
  );
}
